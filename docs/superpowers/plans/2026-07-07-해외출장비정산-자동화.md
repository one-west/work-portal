# 해외출장비 정산 자동화 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 카드 매입/승인 내역 엑셀에서 특정 카드 사용분을 추출해 해외출장비 정산서 템플릿의 `2-1출장경비 법인카드` 탭을 자동으로 채우는 Streamlit 페이지를 추가한다.

**Architecture:** 순수 파이썬 로직(`lib/expense/`: parsers·rules·writer)과 Streamlit UI(`pages/3_해외출장비정산.py`)를 분리한다. 로직은 프레임워크 비의존이라 단위 테스트가 가능하고, UI는 배선만 한다. 엑셀 쓰기는 openpyxl로 내장 템플릿을 열어 detail 셀만 채운다(요약 탭은 템플릿 SUMIFS가 자동 집계).

**Tech Stack:** Python 3, Streamlit, pandas, openpyxl, xlrd(.xls 읽기), pytest.

## Global Constraints

- Python: 이 리포는 `requirements.txt` 기반 (streamlit, pandas, numpy, openpyxl, xlsxwriter, python-dotenv, opendartreader, requests). **xlrd 추가 필요.**
- Streamlit Cloud(리눅스, 엑셀 미설치) 호환만 사용 — LibreOffice/win32 의존 금지. `.xls`는 xlrd로만 읽는다.
- 페이지 파일명은 기존 규칙(`숫자_한글.py`)을 따른다: `pages/3_해외출장비정산.py`.
- 로직 모듈(`lib/expense/*.py`)은 **streamlit을 import 하지 않는다**(테스트 가능성).
- L열(원화금액): 해외 건 = `결제원금`, 국내 건 = `승인금액`.
- 2-1 탭 수식: J=`=IF(H{r}>0,$L{r}/H{r},0)`, K=`=IF(I{r}>0,$L{r}/I{r},0)` (열 절대 `$L`, 행 상대).
- 템플릿 시트명(정확히): `출장비 정산(원화지급원화회수)`, `출장비 정산(외화지급외화회수)`, `2-1출장경비 법인카드`, `2-2출장경비 현금`, `2-3출장경비 개인경비`, `3.간이영수증 샘플`, `4.분류`.
- 4.분류 항목(13개): 식비, 렌터카, 생필품, 소모품비, 여비교통비, 숙박비, 수수료, 의약품 외, 회식비, 접대비, 통신비, 기타, 항공료.

---

### Task 1: 스캐폴딩 — 패키지·의존성·내장 템플릿

**Files:**
- Create: `lib/__init__.py`, `lib/expense/__init__.py`
- Create: `tests/__init__.py`, `tests/expense/__init__.py`
- Create: `templates/해외출장비정산서_v1.3.xlsx` (복사)
- Modify: `requirements.txt` (xlrd, pytest 추가)
- Test: `tests/expense/test_scaffold.py`

**Interfaces:**
- Produces: `templates/해외출장비정산서_v1.3.xlsx` 경로, `lib.expense` 임포트 가능.

- [ ] **Step 1: 디렉터리·빈 파일 생성**

```bash
mkdir -p lib/expense tests/expense templates
touch lib/__init__.py lib/expense/__init__.py tests/__init__.py tests/expense/__init__.py
```

- [ ] **Step 2: 내장 템플릿 복사(원본 무변경본)**

```bash
cp "C:/Users/user/Downloads/해외출장비정산서 업무 자료/3. 해외출장비 정산서_v1.3.xlsx" "templates/해외출장비정산서_v1.3.xlsx"
```

- [ ] **Step 3: requirements.txt에 xlrd, pytest 추가**

`requirements.txt` 끝에 두 줄 추가:
```
xlrd
pytest
```

- [ ] **Step 4: 의존성 설치**

Run: `python -m pip install xlrd pytest openpyxl`
Expected: 정상 설치 (openpyxl은 이미 있을 수 있음)

- [ ] **Step 5: 스캐폴딩 스모크 테스트 작성**

```python
# tests/expense/test_scaffold.py
import os
import importlib

def test_package_imports():
    importlib.import_module("lib.expense")

def test_template_bundled():
    assert os.path.exists("templates/해외출장비정산서_v1.3.xlsx")

def test_template_has_expected_sheets():
    import openpyxl
    wb = openpyxl.load_workbook("templates/해외출장비정산서_v1.3.xlsx")
    assert "2-1출장경비 법인카드" in wb.sheetnames
    assert "출장비 정산(원화지급원화회수)" in wb.sheetnames
```

- [ ] **Step 6: 테스트 실행**

Run: `python -m pytest tests/expense/test_scaffold.py -v`
Expected: 3 passed

- [ ] **Step 7: 커밋**

```bash
git add lib tests templates requirements.txt
git commit -m "chore: expense 자동화 스캐폴딩 + 내장 템플릿 + xlrd 의존성"
```

---

### Task 2: 데이터 모델 (`models.py`)

**Files:**
- Create: `lib/expense/models.py`
- Test: `tests/expense/test_models.py`

**Interfaces:**
- Produces:
  - `Row(date, shop, usd, idr, krw, user, source, card_no, industry="", category="", detail="")`
  - `Meta(traveler="", start_date="", end_date="", region="", purpose="", usd_rate=0.0, idr_rate=0.0)`
  - `CardInfo(card_no, traveler="", region="", label="")`

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/expense/test_models.py
from lib.expense.models import Row, Meta, CardInfo

def test_row_defaults():
    r = Row(date="2026.06.02", shop="ENMARKET", usd=41.65, idr=0,
            krw=64936, user="공용", source="haewoe", card_no="4074672197393852")
    assert r.category == ""
    assert r.detail == ""
    assert r.industry == ""

def test_meta_defaults():
    m = Meta(traveler="김동현")
    assert m.usd_rate == 0.0
    assert m.region == ""

def test_cardinfo():
    c = CardInfo(card_no="4074672197393852", traveler="김동현", region="미국 조지아", label="해외출장4")
    assert c.traveler == "김동현"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_models.py -v`
Expected: FAIL (ModuleNotFoundError: lib.expense.models)

- [ ] **Step 3: 모델 구현**

```python
# lib/expense/models.py
from dataclasses import dataclass

@dataclass
class Row:
    date: str
    shop: str
    usd: float
    idr: float
    krw: float
    user: str
    source: str          # "haewoe" | "gukne"
    card_no: str
    industry: str = ""   # 업종명 (국내 건만)
    category: str = ""   # D열 분류 결과/편집값
    detail: str = ""     # F열 상세내역

@dataclass
class Meta:
    traveler: str = ""
    start_date: str = ""
    end_date: str = ""
    region: str = ""
    purpose: str = ""
    usd_rate: float = 0.0
    idr_rate: float = 0.0

@dataclass
class CardInfo:
    card_no: str
    traveler: str = ""
    region: str = ""
    label: str = ""      # 용도 라벨 (예: 해외출장4)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_models.py -v`
Expected: 3 passed

- [ ] **Step 5: 커밋**

```bash
git add lib/expense/models.py tests/expense/test_models.py
git commit -m "feat: expense 데이터 모델(Row/Meta/CardInfo)"
```

---

### Task 3: 숫자 정규화 `to_number`

**Files:**
- Create: `lib/expense/parsers.py`
- Test: `tests/expense/test_parsers.py`

**Interfaces:**
- Produces: `to_number(x) -> float | None` — 유니코드 공백·콤마·△▲ 음수·괄호 음수·en/em dash 처리. 빈값/파싱불가 → None.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/expense/test_parsers.py
from lib.expense.parsers import to_number

def test_plain_number():
    assert to_number("64,936") == 64936.0

def test_decimal():
    assert to_number("41.65") == 41.65

def test_triangle_negative():
    assert to_number("△1,200") == -1200.0
    assert to_number("▲500") == -500.0

def test_paren_negative():
    assert to_number("(1,000)") == -1000.0

def test_unicode_space():
    assert to_number(" 1,234　") == 1234.0

def test_blank_and_dash():
    assert to_number("") is None
    assert to_number("-") is None
    assert to_number(None) is None
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_parsers.py -v`
Expected: FAIL (ImportError)

- [ ] **Step 3: 구현**

```python
# lib/expense/parsers.py
import re

_UNICODE_SPACES = [" ", "﻿", " ", " ", "⁠",
                   "​", "‌", "‍", "　", "­", "\t"]

def to_number(x):
    """DART/카드 API 숫자 문자열 정규화. 실패 시 None."""
    if x is None:
        return None
    s = str(x)
    for sp in _UNICODE_SPACES:
        s = s.replace(sp, "")
    s = s.replace("–", "-").replace("—", "-")  # en/em dash
    s = s.strip().replace(",", "").replace(" ", "")
    if s in ("", "-", "--"):
        return None
    neg = False
    if s and s[0] in ("△", "▲"):
        neg = True
        s = s[1:]
    if s.startswith("(") and s.endswith(")"):
        neg = True
        s = s[1:-1]
    try:
        val = float(s)
    except ValueError:
        return None
    return -val if neg else val
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_parsers.py -v`
Expected: 6 passed

- [ ] **Step 5: 커밋**

```bash
git add lib/expense/parsers.py tests/expense/test_parsers.py
git commit -m "feat: to_number 숫자 정규화"
```

---

### Task 4: 카드번호 정규화 + 유니크 카드 추출

**Files:**
- Modify: `lib/expense/parsers.py`
- Test: `tests/expense/test_parsers.py`

**Interfaces:**
- Produces:
  - `normalize_card_no(s) -> str` — 숫자만 남김
  - `extract_cards(rows) -> list[str]` — Row 목록에서 유니크 카드번호(등장 순)

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/expense/test_parsers.py 에 추가
from lib.expense.parsers import normalize_card_no, extract_cards
from lib.expense.models import Row

def test_normalize_card_no():
    assert normalize_card_no("4074-6721-9739-3852") == "4074672197393852"
    assert normalize_card_no("4074 6721 9739 3852") == "4074672197393852"

def _row(card):
    return Row(date="2026.06.02", shop="X", usd=1, idr=0, krw=1,
               user="공용", source="haewoe", card_no=card)

def test_extract_cards_unique_ordered():
    rows = [_row("111"), _row("222"), _row("111")]
    assert extract_cards(rows) == ["111", "222"]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k "card" -v`
Expected: FAIL (ImportError)

- [ ] **Step 3: 구현 추가**

```python
# lib/expense/parsers.py 에 추가
def normalize_card_no(s):
    return "".join(ch for ch in str(s) if ch.isdigit())

def extract_cards(rows):
    seen = []
    for r in rows:
        if r.card_no not in seen:
            seen.append(r.card_no)
    return seen
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k "card" -v`
Expected: 2 passed

- [ ] **Step 5: 커밋**

```bash
git add lib/expense/parsers.py tests/expense/test_parsers.py
git commit -m "feat: 카드번호 정규화 + 유니크 카드 추출"
```

---

### Task 5: 해외매입 정규화 `normalize_haewoe`

**Files:**
- Modify: `lib/expense/parsers.py`
- Test: `tests/expense/test_parsers.py`

**Interfaces:**
- Consumes: `to_number`, `normalize_card_no`, `Row`
- Produces: `normalize_haewoe(df) -> list[Row]` — 해외매입 DataFrame(문자열)을 통합 Row로. 열: 승인일→date, 가맹점명→shop, 카드번호→card_no, 현지이용금액→usd, 결제원금→krw, 이용자명→user, idr=0, source="haewoe".
  - `read_haewoe(path) -> DataFrame` — `pd.read_excel(path, engine="xlrd", dtype=str)`

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/expense/test_parsers.py 에 추가
import pandas as pd
from lib.expense.parsers import normalize_haewoe

def test_normalize_haewoe():
    df = pd.DataFrame([
        {"승인일": "2026.06.27", "가맹점명": "ENMARKET 1330", "카드번호": "4074-6721-9739-3852",
         "이용자명": "공용", "현지이용금액": "41.65", "결제원금": "64,936"},
    ])
    rows = normalize_haewoe(df)
    assert len(rows) == 1
    r = rows[0]
    assert r.source == "haewoe"
    assert r.card_no == "4074672197393852"
    assert r.usd == 41.65
    assert r.idr == 0
    assert r.krw == 64936.0
    assert r.shop == "ENMARKET 1330"
    assert r.user == "공용"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k haewoe -v`
Expected: FAIL (ImportError)

- [ ] **Step 3: 구현 추가**

```python
# lib/expense/parsers.py 상단 import 에 추가
import pandas as pd
from lib.expense.models import Row

# 함수 추가
def read_haewoe(path):
    return pd.read_excel(path, engine="xlrd", dtype=str)

def normalize_haewoe(df):
    rows = []
    for _, x in df.iterrows():
        rows.append(Row(
            date=str(x.get("승인일", "")).strip(),
            shop=str(x.get("가맹점명", "")).strip(),
            usd=to_number(x.get("현지이용금액")) or 0.0,
            idr=0.0,
            krw=to_number(x.get("결제원금")) or 0.0,
            user=str(x.get("이용자명", "")).strip(),
            source="haewoe",
            card_no=normalize_card_no(x.get("카드번호", "")),
        ))
    return rows
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k haewoe -v`
Expected: 1 passed

- [ ] **Step 5: 커밋**

```bash
git add lib/expense/parsers.py tests/expense/test_parsers.py
git commit -m "feat: 해외매입 정규화 normalize_haewoe"
```

---

### Task 6: 국내승인 정규화 `normalize_gukne` (취소 필터 + 업종명)

**Files:**
- Modify: `lib/expense/parsers.py`
- Test: `tests/expense/test_parsers.py`

**Interfaces:**
- Consumes: `to_number`, `normalize_card_no`, `Row`
- Produces: `normalize_gukne(df) -> list[Row]` — 열: 승인일→date, 가맹점명→shop, 업종명→industry, 카드번호→card_no, 승인금액→krw, 이용자명→user, usd=idr=0, source="gukne". `상태`/`승인구분`에 "취소"/"거절" 포함 행은 제외.
  - `read_gukne(path) -> DataFrame`

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/expense/test_parsers.py 에 추가
from lib.expense.parsers import normalize_gukne

def test_normalize_gukne_maps_and_filters_cancel():
    df = pd.DataFrame([
        {"승인일": "2026.06.01", "가맹점명": "GS25", "업종명": "편의점", "카드번호": "111",
         "이용자명": "공용", "승인금액": "10,000", "상태": "정상"},
        {"승인일": "2026.06.02", "가맹점명": "취소건", "업종명": "편의점", "카드번호": "111",
         "이용자명": "공용", "승인금액": "5,000", "상태": "취소"},
    ])
    rows = normalize_gukne(df)
    assert len(rows) == 1          # 취소건 제외
    r = rows[0]
    assert r.source == "gukne"
    assert r.industry == "편의점"
    assert r.krw == 10000.0
    assert r.usd == 0 and r.idr == 0
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k gukne -v`
Expected: FAIL (ImportError)

- [ ] **Step 3: 구현 추가**

```python
# lib/expense/parsers.py 에 추가
def read_gukne(path):
    return pd.read_excel(path, engine="xlrd", dtype=str)

def _is_cancelled(x):
    for col in ("상태", "승인구분"):
        v = str(x.get(col, ""))
        if "취소" in v or "거절" in v:
            return True
    return False

def normalize_gukne(df):
    rows = []
    for _, x in df.iterrows():
        if _is_cancelled(x):
            continue
        rows.append(Row(
            date=str(x.get("승인일", "")).strip(),
            shop=str(x.get("가맹점명", "")).strip(),
            usd=0.0,
            idr=0.0,
            krw=to_number(x.get("승인금액")) or 0.0,
            user=str(x.get("이용자명", "")).strip(),
            source="gukne",
            card_no=normalize_card_no(x.get("카드번호", "")),
            industry=str(x.get("업종명", "")).strip(),
        ))
    return rows
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k gukne -v`
Expected: 1 passed

- [ ] **Step 5: 커밋**

```bash
git add lib/expense/parsers.py tests/expense/test_parsers.py
git commit -m "feat: 국내승인 정규화 + 취소 필터"
```

---

### Task 7: 카드 마스터 파싱 `parse_card_master`

**Files:**
- Modify: `lib/expense/parsers.py`
- Test: `tests/expense/test_parsers.py`

**Interfaces:**
- Consumes: `normalize_card_no`, `CardInfo`
- Produces: `parse_card_master(df) -> dict[str, CardInfo]` — 헤더 키워드로 컬럼 자동탐지(카드번호/출장자·이름·성명/지역/용도·비고). 키=정규화 카드번호.
  - `read_card_master(path) -> DataFrame` — 확장자에 따라 xls/xlsx/csv 읽기.

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/expense/test_parsers.py 에 추가
from lib.expense.parsers import parse_card_master

def test_parse_card_master_detects_columns():
    df = pd.DataFrame([
        {"카드번호": "4074-6721-9739-3852", "용도": "해외출장4", "출장자명": "김동현", "지역": "미국 조지아"},
        {"카드번호": "4074-6752-5283-8828", "용도": "해외출장14", "출장자명": "권용건", "지역": "미국 조지아"},
    ])
    m = parse_card_master(df)
    info = m["4074672197393852"]
    assert info.traveler == "김동현"
    assert info.region == "미국 조지아"
    assert info.label == "해외출장4"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k card_master -v`
Expected: FAIL (ImportError)

- [ ] **Step 3: 구현 추가**

```python
# lib/expense/parsers.py 상단 import 에 CardInfo 추가
from lib.expense.models import Row, CardInfo

def _find_col(df, keywords):
    for c in df.columns:
        name = str(c)
        if any(k in name for k in keywords):
            return c
    return None

def read_card_master(path):
    p = str(path).lower()
    if p.endswith(".csv"):
        return pd.read_csv(path, dtype=str)
    if p.endswith(".xls"):
        return pd.read_excel(path, engine="xlrd", dtype=str)
    return pd.read_excel(path, dtype=str)

def parse_card_master(df):
    col_card = _find_col(df, ["카드번호"])
    col_name = _find_col(df, ["출장자", "이름", "성명"])
    col_region = _find_col(df, ["지역", "출장지"])
    col_label = _find_col(df, ["용도", "비고", "구분"])
    out = {}
    if col_card is None:
        return out
    for _, x in df.iterrows():
        key = normalize_card_no(x.get(col_card, ""))
        if not key:
            continue
        out[key] = CardInfo(
            card_no=key,
            traveler=str(x.get(col_name, "")).strip() if col_name else "",
            region=str(x.get(col_region, "")).strip() if col_region else "",
            label=str(x.get(col_label, "")).strip() if col_label else "",
        )
    return out
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k card_master -v`
Expected: 1 passed

- [ ] **Step 5: 커밋**

```bash
git add lib/expense/parsers.py tests/expense/test_parsers.py
git commit -m "feat: 카드 마스터 파싱(컬럼 자동탐지)"
```

---

### Task 8: 카드별 필터 + 정렬 `filter_and_sort`

**Files:**
- Modify: `lib/expense/parsers.py`
- Test: `tests/expense/test_parsers.py`

**Interfaces:**
- Consumes: `Row`
- Produces: `filter_and_sort(rows, card_no) -> list[Row]` — 카드 일치분만, `date` 오름차순 안정정렬.

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/expense/test_parsers.py 에 추가
from lib.expense.parsers import filter_and_sort

def test_filter_and_sort():
    rows = [
        Row(date="2026.06.05", shop="B", usd=1, idr=0, krw=1, user="", source="haewoe", card_no="111"),
        Row(date="2026.06.02", shop="A", usd=1, idr=0, krw=1, user="", source="haewoe", card_no="111"),
        Row(date="2026.06.03", shop="C", usd=1, idr=0, krw=1, user="", source="haewoe", card_no="999"),
    ]
    out = filter_and_sort(rows, "111")
    assert [r.shop for r in out] == ["A", "B"]
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k filter_and_sort -v`
Expected: FAIL (ImportError)

- [ ] **Step 3: 구현 추가**

```python
# lib/expense/parsers.py 에 추가
def filter_and_sort(rows, card_no):
    subset = [r for r in rows if r.card_no == card_no]
    return sorted(subset, key=lambda r: r.date)   # 파이썬 sort는 안정정렬
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_parsers.py -k filter_and_sort -v`
Expected: 1 passed

- [ ] **Step 5: 커밋**

```bash
git add lib/expense/parsers.py tests/expense/test_parsers.py
git commit -m "feat: 카드별 필터 + 날짜 정렬"
```

---

### Task 9: 규칙 모델 + 기본셋 + JSON 라운드트립

**Files:**
- Create: `lib/expense/rules.py`
- Create: `lib/expense/rules_default.json`
- Test: `tests/expense/test_rules.py`

**Interfaces:**
- Consumes: (없음)
- Produces:
  - `CATEGORIES: list[str]` (4.분류 13개)
  - `Rule(order:int, match:str, keyword:str, category:str, applies_to:str, note:str="")`
  - `default_rules() -> list[Rule]` (rules_default.json 로드)
  - `load_rules(obj) -> list[Rule]`, `dump_rules(rules) -> dict`
  - `validate_category(cat) -> bool`

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/expense/test_rules.py
from lib.expense.rules import (
    CATEGORIES, Rule, default_rules, load_rules, dump_rules, validate_category,
)

def test_categories_has_expected():
    for c in ["식비", "여비교통비", "소모품비", "접대비", "항공료"]:
        assert c in CATEGORIES

def test_default_rules_nonempty_and_ordered():
    rules = default_rules()
    assert len(rules) > 0
    orders = [r.order for r in rules]
    assert orders == sorted(orders)

def test_json_roundtrip():
    rules = default_rules()
    obj = dump_rules(rules)
    back = load_rules(obj)
    assert [r.keyword for r in back] == [r.keyword for r in rules]
    assert [r.category for r in back] == [r.category for r in rules]

def test_validate_category():
    assert validate_category("식비") is True
    assert validate_category("") is True          # 공란 허용
    assert validate_category("없는항목") is False
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_rules.py -v`
Expected: FAIL (ImportError)

- [ ] **Step 3: rules_default.json 작성**

```json
{
  "version": 1,
  "rules": [
    {"order": 1, "match": "contains", "keyword": "FOOD LION|COSTCO|WAL-MART|WALMART|KROGER|PUBLIX|TARGET", "category": "", "applies_to": "haewoe", "note": "마트 대량구매 유보"},
    {"order": 2, "match": "contains", "keyword": "SPBU|SHELL|EXXON|CHEVRON|WAWA|SUNOCO|GAS|FUEL|PARKING|UBER|LYFT|TAXI|TOLL", "category": "여비교통비", "applies_to": "common", "note": ""},
    {"order": 3, "match": "contains", "keyword": "FASTENAL|HOME DEPOT|LOWES|GRAINGER|HARDWARE", "category": "소모품비", "applies_to": "common", "note": ""},
    {"order": 4, "match": "contains", "keyword": "AIRLINE|DELTA|UNITED AIR|KOREAN AIR", "category": "항공료", "applies_to": "common", "note": ""},
    {"order": 5, "match": "contains", "keyword": "HOTEL|MOTEL|MARRIOTT|HILTON|HYATT| INN", "category": "숙박비", "applies_to": "common", "note": ""},
    {"order": 6, "match": "contains", "keyword": "ROAMING|AT&T|VERIZON|T-MOBILE|SIM", "category": "통신비", "applies_to": "common", "note": ""},
    {"order": 7, "match": "contains", "keyword": "365 MARKET|MCDONALD|WENDY|CHICK|SUBWAY|BURGER|PIZZA|CAFE|CAFFE|RESTAURANT|ZIPBOB|LOW COUNTRY|ENMARKET|STARBUCK|KFC|GRILL|BBQ|COFFEE|DINER|TACO|CHIPOTLE|DUNKIN", "category": "식비", "applies_to": "haewoe", "note": ""},
    {"order": 8, "match": "contains", "keyword": "편의점|음식|식당|주유|마트|카페", "category": "식비", "applies_to": "gukne", "note": "국내 업종명 기반(대략)"},
    {"order": 9, "match": "regex", "keyword": "^[A-Z]{2,4}\\d{4,}$", "category": "", "applies_to": "haewoe", "note": "코드성 상호"}
  ]
}
```

- [ ] **Step 4: rules.py 구현**

```python
# lib/expense/rules.py
import json
import os
from dataclasses import dataclass, asdict

CATEGORIES = [
    "식비", "렌터카", "생필품", "소모품비", "여비교통비", "숙박비", "수수료",
    "의약품 외", "회식비", "접대비", "통신비", "기타", "항공료",
]

_DEFAULT_PATH = os.path.join(os.path.dirname(__file__), "rules_default.json")

@dataclass
class Rule:
    order: int
    match: str          # "contains" | "regex"
    keyword: str
    category: str
    applies_to: str     # "haewoe" | "gukne" | "common"
    note: str = ""

def load_rules(obj):
    data = json.loads(obj) if isinstance(obj, str) else obj
    rules = [Rule(**r) for r in data["rules"]]
    return sorted(rules, key=lambda r: r.order)

def dump_rules(rules):
    return {"version": 1, "rules": [asdict(r) for r in rules]}

def default_rules():
    with open(_DEFAULT_PATH, encoding="utf-8") as f:
        return load_rules(f.read())

def validate_category(cat):
    return cat == "" or cat in CATEGORIES
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_rules.py -v`
Expected: 4 passed

- [ ] **Step 6: 커밋**

```bash
git add lib/expense/rules.py lib/expense/rules_default.json tests/expense/test_rules.py
git commit -m "feat: 분류 규칙 모델 + 기본셋 + JSON 라운드트립"
```

---

### Task 10: 분류 `classify` (원천별 분기·순서·contains/regex)

**Files:**
- Modify: `lib/expense/rules.py`
- Test: `tests/expense/test_rules.py`

**Interfaces:**
- Consumes: `Rule`, `Row`
- Produces:
  - `classify(row, rules) -> str` — 대상 텍스트=(gukne면 industry, 아니면 shop) 대문자화; `applies_to ∈ {row.source, "common"}` 규칙을 order순 검사; contains는 `|` 분리 다중키워드 부분일치, regex는 정규식; 첫 매칭 category, 없으면 "".
  - `classify_rows(rows, rules) -> list[Row]` — 각 Row.category 설정(제자리 수정 후 반환).

- [ ] **Step 1: 실패 테스트 추가**

```python
# tests/expense/test_rules.py 에 추가
from lib.expense.rules import classify, classify_rows
from lib.expense.models import Row

def _h(shop):
    return Row(date="d", shop=shop, usd=1, idr=0, krw=1, user="", source="haewoe", card_no="1")

def _g(industry):
    return Row(date="d", shop="X", usd=0, idr=0, krw=1, user="", source="gukne", card_no="1", industry=industry)

def test_classify_haewoe_keyword():
    rules = default_rules()
    assert classify(_h("MCDONALD'S F39732"), rules) == "식비"
    assert classify(_h("WAWA 6309"), rules) == "여비교통비"     # 순서: 마트 뒤 주유 먼저
    assert classify(_h("FASTENAL COMPANY"), rules) == "소모품비"

def test_classify_mart_blank():
    assert classify(_h("FOOD LION #2811"), default_rules()) == ""

def test_classify_code_regex_blank():
    assert classify(_h("EOC03171"), default_rules()) == ""

def test_classify_gukne_uses_industry():
    assert classify(_g("편의점"), default_rules()) == "식비"

def test_classify_rows_sets_category():
    rows = [_h("MCDONALD'S"), _h("FOOD LION")]
    out = classify_rows(rows, default_rules())
    assert out[0].category == "식비"
    assert out[1].category == ""
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_rules.py -k classify -v`
Expected: FAIL (ImportError)

- [ ] **Step 3: 구현 추가**

```python
# lib/expense/rules.py 상단에 추가
import re

# 함수 추가
def _matches(rule, text):
    if rule.match == "regex":
        return re.search(rule.keyword, text) is not None
    return any(k and k in text for k in rule.keyword.split("|"))

def classify(row, rules):
    text = (row.industry if row.source == "gukne" else row.shop).upper()
    for rule in sorted(rules, key=lambda r: r.order):
        if rule.applies_to not in (row.source, "common"):
            continue
        if _matches(rule, text):
            return rule.category
    return ""

def classify_rows(rows, rules):
    for r in rows:
        r.category = classify(r, rules)
    return rows
```

주의: `_matches`의 contains는 대문자 text 기준이라 국내 한글 키워드(편의점 등)는 대문자 영향 없음. 규칙 keyword도 그대로 부분일치.

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_rules.py -k classify -v`
Expected: 5 passed

- [ ] **Step 5: 커밋**

```bash
git add lib/expense/rules.py tests/expense/test_rules.py
git commit -m "feat: classify 분류(원천별 분기·순서·contains/regex)"
```

---

### Task 11: 엑셀 라이터 `build_settlement`

**Files:**
- Create: `lib/expense/writer.py`
- Test: `tests/expense/test_writer.py`

**Interfaces:**
- Consumes: `Row`, `Meta`
- Produces:
  - `TEMPLATE_PATH: str` — 내장 템플릿 경로
  - `build_settlement(rows, meta, template_path=TEMPLATE_PATH) -> bytes` — 2-1 탭에 rows 기입 + 요약탭에 meta/환율 기입 → xlsx 바이트.

- [ ] **Step 1: 실패 테스트 작성**

```python
# tests/expense/test_writer.py
import io
import openpyxl
from lib.expense.models import Row, Meta
from lib.expense.writer import build_settlement

def _rows(n):
    out = []
    for i in range(n):
        out.append(Row(date=f"2026.06.{i+1:02d}", shop=f"SHOP{i}", usd=10.0, idr=0,
                       krw=15000, user="김동현", source="haewoe", card_no="1",
                       category="식비", detail=""))
    return out

def test_build_fills_2_1_rows_and_total():
    rows = _rows(3)
    meta = Meta(traveler="김동현", start_date="2026.06.01", end_date="2026.06.30",
                region="미국 조지아", purpose="현장지원", usd_rate=1470, idr_rate=0.09)
    data = build_settlement(rows, meta)
    wb = openpyxl.load_workbook(io.BytesIO(data))
    ws = wb["2-1출장경비 법인카드"]
    # 데이터 9~11, 합계 12
    assert ws.cell(9, 3).value == "SHOP0"          # C9 상호
    assert ws.cell(9, 12).value == 15000           # L9 원화금액
    assert ws.cell(9, 13).value == "김동현"         # M9 사용자
    assert ws.cell(9, 4).value == "식비"            # D9 항목
    assert ws.cell(9, 10).value == "=IF(H9>0,$L9/H9,0)"   # J9 수식
    assert ws.cell(12, 1).value == "합     계"
    assert ws.cell(12, 12).value == "=SUM(L9:L11)"

def test_build_writes_meta():
    data = build_settlement(_rows(1),
                            Meta(traveler="김동현", region="미국 조지아", usd_rate=1470, idr_rate=0.09))
    wb = openpyxl.load_workbook(io.BytesIO(data))
    ws = wb["출장비 정산(원화지급원화회수)"]
    assert ws.cell(6, 4).value == "김동현"          # D6 출장자
    assert ws.cell(5, 9).value == 1470              # I5 USD 환율
    assert ws.cell(6, 9).value == 0.09              # I6 IDR 환율
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `python -m pytest tests/expense/test_writer.py -v`
Expected: FAIL (ImportError)

- [ ] **Step 3: writer.py 구현**

```python
# lib/expense/writer.py
import io
import os
from copy import copy
import openpyxl

TEMPLATE_PATH = os.path.join(
    os.path.dirname(os.path.dirname(os.path.dirname(__file__))),
    "templates", "해외출장비정산서_v1.3.xlsx",
)

_SHEET_2_1 = "2-1출장경비 법인카드"
_SUMMARY_TABS = ["출장비 정산(원화지급원화회수)", "출장비 정산(외화지급외화회수)"]
_DATA_START = 9
_REF_DATA = 11     # 원본 데이터행(스타일 참조)
_REF_TOTAL = 31    # 원본 합계행(스타일 참조)

def _fill_2_1(ws, rows):
    n = len(rows)
    data_end = _DATA_START + n - 1
    total_row = data_end + 1

    rds = {c: copy(ws.cell(_REF_DATA, c)._style) for c in range(1, 14)}
    rdf = {c: ws.cell(_REF_DATA, c).number_format for c in range(1, 14)}
    rdh = ws.row_dimensions[_REF_DATA].height
    rts = {c: copy(ws.cell(_REF_TOTAL, c)._style) for c in range(1, 14)}
    rtf = {c: ws.cell(_REF_TOTAL, c).number_format for c in range(1, 14)}
    rth = ws.row_dimensions[_REF_TOTAL].height

    for m in list(ws.merged_cells.ranges):
        if m.min_row >= _DATA_START:
            ws.unmerge_cells(str(m))
    for r in range(_DATA_START, max(total_row + 2, _REF_TOTAL + 2)):
        for c in range(1, 15):
            ws.cell(r, c).value = None

    for i, row in enumerate(rows):
        r = _DATA_START + i
        ws.row_dimensions[r].height = rdh
        for c in range(1, 14):
            cell = ws.cell(r, c)
            cell._style = copy(rds[c])
            cell.number_format = rdf[c]
        ws.cell(r, 1).value = "=ROW()-8"
        ws.cell(r, 2).value = row.date
        ws.cell(r, 3).value = row.shop
        ws.cell(r, 4).value = row.category or None
        ws.cell(r, 5).value = "법인카드"
        ws.cell(r, 6).value = row.detail or None
        ws.cell(r, 8).value = float(row.usd)
        ws.cell(r, 9).value = float(row.idr)
        ws.cell(r, 10).value = f"=IF(H{r}>0,$L{r}/H{r},0)"
        ws.cell(r, 11).value = f"=IF(I{r}>0,$L{r}/I{r},0)"
        ws.cell(r, 12).value = int(round(row.krw))
        ws.cell(r, 13).value = row.user or "공용"
        ws.merge_cells(start_row=r, start_column=6, end_row=r, end_column=7)

    ws.row_dimensions[total_row].height = rth
    for c in range(1, 14):
        cell = ws.cell(total_row, c)
        cell._style = copy(rts[c])
        cell.number_format = rtf[c]
    ws.cell(total_row, 1).value = "합     계"
    ws.cell(total_row, 8).value = f"=SUM(H{_DATA_START}:H{data_end})"
    ws.cell(total_row, 9).value = f"=SUM(I{_DATA_START}:I{data_end})"
    ws.cell(total_row, 12).value = f"=SUM(L{_DATA_START}:L{data_end})"
    ws.merge_cells(start_row=total_row, start_column=1, end_row=total_row, end_column=3)
    ws.merge_cells(start_row=total_row, start_column=6, end_row=total_row, end_column=7)

def _fill_meta(wb, meta):
    for name in _SUMMARY_TABS:
        if name not in wb.sheetnames:
            continue
        ws = wb[name]
        ws.cell(6, 4).value = meta.traveler        # D6 출장자
        ws.cell(7, 4).value = meta.start_date      # D7 기간 시작
        ws.cell(7, 5).value = meta.end_date        # E7 기간 종료
        ws.cell(8, 4).value = meta.region          # D8 지역/업체
        ws.cell(9, 4).value = meta.purpose         # D9 목적
        ws.cell(5, 9).value = meta.usd_rate        # I5 USD 환율
        ws.cell(6, 9).value = meta.idr_rate        # I6 IDR 환율

def build_settlement(rows, meta, template_path=TEMPLATE_PATH):
    wb = openpyxl.load_workbook(template_path)
    if _SHEET_2_1 not in wb.sheetnames:
        raise ValueError(f"템플릿에 '{_SHEET_2_1}' 시트가 없습니다.")
    _fill_2_1(wb[_SHEET_2_1], rows)
    _fill_meta(wb, meta)
    buf = io.BytesIO()
    wb.save(buf)
    return buf.getvalue()
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `python -m pytest tests/expense/test_writer.py -v`
Expected: 2 passed

- [ ] **Step 5: 커밋**

```bash
git add lib/expense/writer.py tests/expense/test_writer.py
git commit -m "feat: build_settlement 엑셀 라이터(2-1 채움 + 메타/환율)"
```

---

### Task 12: 공개 API + 엔드투엔드 통합 테스트

**Files:**
- Modify: `lib/expense/__init__.py`
- Test: `tests/expense/test_integration.py`

**Interfaces:**
- Produces: `lib.expense`에서 `to_number, normalize_card_no, extract_cards, normalize_haewoe, normalize_gukne, read_haewoe, read_gukne, read_card_master, parse_card_master, filter_and_sort, default_rules, load_rules, dump_rules, classify, classify_rows, CATEGORIES, Rule, build_settlement, Row, Meta, CardInfo` 노출.

- [ ] **Step 1: __init__.py 공개 API 작성**

```python
# lib/expense/__init__.py
from lib.expense.models import Row, Meta, CardInfo
from lib.expense.parsers import (
    to_number, normalize_card_no, extract_cards,
    normalize_haewoe, normalize_gukne, read_haewoe, read_gukne,
    read_card_master, parse_card_master, filter_and_sort,
)
from lib.expense.rules import (
    CATEGORIES, Rule, default_rules, load_rules, dump_rules,
    validate_category, classify, classify_rows,
)
from lib.expense.writer import build_settlement, TEMPLATE_PATH

__all__ = [
    "Row", "Meta", "CardInfo",
    "to_number", "normalize_card_no", "extract_cards",
    "normalize_haewoe", "normalize_gukne", "read_haewoe", "read_gukne",
    "read_card_master", "parse_card_master", "filter_and_sort",
    "CATEGORIES", "Rule", "default_rules", "load_rules", "dump_rules",
    "validate_category", "classify", "classify_rows",
    "build_settlement", "TEMPLATE_PATH",
]
```

- [ ] **Step 2: 통합 테스트 작성 (DataFrame → 정규화 → 분류 → 쓰기 → 재로드 → 합계 검증)**

```python
# tests/expense/test_integration.py
import io
import pandas as pd
import openpyxl
from lib import expense

def test_end_to_end_haewoe():
    df = pd.DataFrame([
        {"승인일": "2026.06.27", "가맹점명": "ENMARKET 1330", "카드번호": "4074-6721-9739-3852",
         "이용자명": "공용", "현지이용금액": "41.65", "결제원금": "64,936"},
        {"승인일": "2026.06.02", "가맹점명": "FOOD LION #2811", "카드번호": "4074-6721-9739-3852",
         "이용자명": "공용", "현지이용금액": "541.78", "결제원금": "829,085"},
        {"승인일": "2026.06.05", "가맹점명": "WAWA 6309", "카드번호": "4074-6721-9739-3852",
         "이용자명": "공용", "현지이용금액": "44.00", "결제원금": "68,547"},
    ])
    rows = expense.normalize_haewoe(df)
    card = expense.extract_cards(rows)[0]
    rows = expense.filter_and_sort(rows, card)
    rows = expense.classify_rows(rows, expense.default_rules())
    # 마스터로 사용자명 주입
    for r in rows:
        r.user = "김동현"
    meta = expense.Meta(traveler="김동현", region="미국 조지아", usd_rate=1470, idr_rate=0.09)
    data = expense.build_settlement(rows, meta)

    wb = openpyxl.load_workbook(io.BytesIO(data))
    ws = wb["2-1출장경비 법인카드"]
    # 날짜 오름차순: 06.02(FOOD LION) → 06.05(WAWA) → 06.27(ENMARKET)
    assert ws.cell(9, 3).value == "FOOD LION #2811"
    assert ws.cell(9, 4).value is None            # 마트 → 공란
    assert ws.cell(10, 3).value == "WAWA 6309"
    assert ws.cell(10, 4).value == "여비교통비"
    assert ws.cell(11, 4).value == "식비"          # ENMARKET
    assert ws.cell(12, 12).value == "=SUM(L9:L11)"
    # 원화 합계 검산
    total = sum(r.krw for r in rows)
    assert total == 64936 + 829085 + 68547
```

- [ ] **Step 3: 테스트 실행**

Run: `python -m pytest tests/expense/ -v`
Expected: 전체 통과 (통합 포함)

- [ ] **Step 4: 커밋**

```bash
git add lib/expense/__init__.py tests/expense/test_integration.py
git commit -m "feat: expense 공개 API + 엔드투엔드 통합 테스트"
```

---

### Task 13: Streamlit 페이지 배선 `pages/3_해외출장비정산.py`

**Files:**
- Create: `pages/3_해외출장비정산.py`
- (수동 검증) — 단위 테스트 없음. 로직은 Task 3~12에서 검증됨. 여기서는 UI 배선 + 수동 확인.

**Interfaces:**
- Consumes: `lib.expense`의 공개 API 전체.

- [ ] **Step 1: 페이지 구현**

```python
# pages/3_해외출장비정산.py
import io
import zipfile
import pandas as pd
import streamlit as st
from lib import expense

st.set_page_config(page_title="해외출장비 정산", layout="wide")
st.title("해외출장비 정산서 자동 작성")

st.markdown("카드 매입/승인 내역과 카드 마스터를 올리면 **2-1 법인카드 탭**을 채운 정산서를 생성합니다.")

# ── 1) 업로드 ──
c1, c2, c3 = st.columns(3)
f_haewoe = c1.file_uploader("해외매입내역 (.xls)", type=["xls"])
f_gukne = c2.file_uploader("국내승인내역 (.xls, 선택)", type=["xls"])
f_master = c3.file_uploader("카드 마스터 (.xls/.xlsx/.csv)", type=["xls", "xlsx", "csv"])

if not f_haewoe and not f_gukne:
    st.info("해외매입 또는 국내승인 중 최소 하나를 업로드하세요.")
    st.stop()

# ── 2) 파싱 ──
rows = []
if f_haewoe:
    rows += expense.normalize_haewoe(pd.read_excel(f_haewoe, engine="xlrd", dtype=str))
if f_gukne:
    rows += expense.normalize_gukne(pd.read_excel(f_gukne, engine="xlrd", dtype=str))

master = {}
if f_master:
    master = expense.parse_card_master(expense.read_card_master(f_master))

# ── 3) 규칙 (세션 + 다운/업) ──
with st.expander("분류 규칙 편집"):
    up = st.file_uploader("규칙 JSON 업로드", type=["json"], key="rules_up")
    if up:
        st.session_state["rules"] = expense.load_rules(up.getvalue().decode("utf-8"))
    if "rules" not in st.session_state:
        st.session_state["rules"] = expense.default_rules()
    rules_df = pd.DataFrame([r.__dict__ for r in st.session_state["rules"]])
    edited = st.data_editor(rules_df, num_rows="dynamic", use_container_width=True,
                            column_config={"category": st.column_config.SelectboxColumn(
                                options=[""] + expense.CATEGORIES)})
    st.session_state["rules"] = [expense.Rule(**r) for r in edited.to_dict("records")]
    import json
    st.download_button("규칙 JSON 다운로드",
                       data=json.dumps(expense.dump_rules(st.session_state["rules"]),
                                       ensure_ascii=False, indent=2),
                       file_name="expense_rules.json")

rules = st.session_state["rules"]

# ── 4) 카드 다중선택 (마스터 라벨) ──
def label(card):
    info = master.get(card)
    if info and (info.label or info.traveler):
        return f"{info.label} · {info.traveler} · {info.region} ({card[-4:]})".strip(" ·")
    return card

cards = expense.extract_cards(rows)
picked = st.multiselect("정산할 카드 선택", options=cards, format_func=label)
if not picked:
    st.stop()

# ── 5) 카드별 메타 입력표 (마스터 프리필) ──
st.subheader("카드별 출장 정보")
meta_rows = []
for card in picked:
    info = master.get(card, expense.CardInfo(card_no=card))
    meta_rows.append({"카드": label(card), "card_no": card, "출장자": info.traveler,
                      "기간시작": "", "기간종료": "", "지역": info.region, "목적": "",
                      "USD환율": 1470.0, "IDR환율": 0.09})
meta_df = st.data_editor(pd.DataFrame(meta_rows), use_container_width=True,
                         column_config={"card_no": None}, hide_index=True)

# ── 6) 카드별 편집표 + 생성 ──
files = {}
for _, mrow in meta_df.iterrows():
    card = mrow["card_no"]
    crows = expense.filter_and_sort(rows, card)
    crows = expense.classify_rows(crows, rules)
    for r in crows:
        if not r.user or r.user == "공용":
            r.user = mrow["출장자"] or "공용"
    st.markdown(f"**{label(card)}** — {len(crows)}건")
    detail_df = pd.DataFrame([{"날짜": r.date, "상호": r.shop, "항목": r.category,
                               "상세내역": r.detail, "USD": r.usd, "원화": r.krw,
                               "사용자": r.user} for r in crows])
    edited_detail = st.data_editor(detail_df, use_container_width=True, hide_index=True,
                                   key=f"detail_{card}",
                                   column_config={"항목": st.column_config.SelectboxColumn(
                                       options=[""] + expense.CATEGORIES)})
    # 편집 반영
    for r, (_, d) in zip(crows, edited_detail.iterrows()):
        r.category, r.detail, r.user = d["항목"], d["상세내역"], d["사용자"]
    meta = expense.Meta(traveler=mrow["출장자"], start_date=str(mrow["기간시작"]),
                        end_date=str(mrow["기간종료"]), region=mrow["지역"],
                        purpose=mrow["목적"], usd_rate=float(mrow["USD환율"]),
                        idr_rate=float(mrow["IDR환율"]))
    files[card] = (mrow["출장자"] or card, expense.build_settlement(crows, meta))

# ── 7) 다운로드 (단일=xlsx, 다중=ZIP) ──
st.subheader("다운로드")
if len(files) == 1:
    name, data = next(iter(files.values()))
    st.download_button(f"정산서 다운로드 ({name})", data=data,
                       file_name=f"해외출장비 정산서_{name}.xlsx")
else:
    zbuf = io.BytesIO()
    with zipfile.ZipFile(zbuf, "w") as zf:
        for card, (name, data) in files.items():
            zf.writestr(f"해외출장비 정산서_{name}.xlsx", data)
    st.download_button("정산서 전체 ZIP 다운로드", data=zbuf.getvalue(),
                       file_name="해외출장비 정산서_일괄.zip")
```

- [ ] **Step 2: 로컬 실행**

Run: `streamlit run app.py`
Expected: 사이드바에 "해외출장비정산" 페이지 노출, 에러 없이 로드.

- [ ] **Step 3: 수동 검증 (실제 파일)**

다음을 확인:
1. `1. 해외매입내역.xls` 업로드 → 카드 드롭다운에 카드 목록 표시
2. 카드 마스터 업로드 → 드롭다운 라벨이 "해외출장4 · 김동현 · 미국 조지아" 형태
3. `4074-6721-9739-3852` 선택 → 46건, 자동분류(식비 40/여비교통비 1/소모품비 1/공란 4)
4. 편집표에서 D열 selectbox 동작, 값 수정 반영
5. 다운로드한 xlsx를 엑셀로 열어 요약탭 합계가 원화 2,632,824로 자동 계산되는지
6. 카드 2개 선택 → ZIP로 2개 파일

- [ ] **Step 4: 커밋**

```bash
git add pages/3_해외출장비정산.py
git commit -m "feat: 해외출장비 정산 Streamlit 페이지"
```

---

## Self-Review

**1. Spec coverage:**
- §5 파일구조 → Task 1·2·9·11·13에서 생성. ✅
- §6 데이터흐름 ①업로드 → Task 13 Step1; ②카드선택 라벨 → Task 7·13; ③분류 → Task 10; ④메타 → Task 13; ⑤편집 → Task 13; ⑥생성 → Task 11; ⑦다운로드 ZIP → Task 13. ✅
- §7 규칙모델/JSON/원천분기/검증 → Task 9·10. ✅
- §8 writer 상세(행확장·병합·수식$L·합계·메타) → Task 11. ✅
- §9 에러: 둘다미업로드(Task13 stop)·취소필터(Task6)·숫자정규화(Task3)·카드마스터컬럼탐지(Task7)·마스터없음폴백(Task13 label)·템플릿검증(Task11). ✅ (0나눗셈은 J/K IF가드 Task11.)
- §10 테스트: parsers/rules/writer/통합 → Task 3~12. ✅
- L열 정의(해외 결제원금/국내 승인금액) → Task5·6. ✅

**2. Placeholder scan:** TBD/TODO 없음. 모든 코드 스텝에 실제 코드 포함.

**3. Type consistency:** `Row/Meta/CardInfo` 필드명이 parsers·rules·writer·page에서 일관. `classify(row, rules)`·`classify_rows(rows, rules)`·`build_settlement(rows, meta, template_path)` 시그니처 태스크 간 일치. `default_rules()`/`load_rules()`/`dump_rules()` 이름 일관.

**미해결 리스크(스펙 §11 명시):** 카드 마스터 실제 헤더, 국내 실데이터 검증은 실제 파일 확보 시 Task 7·6의 컬럼/키워드 조정 필요. 국내 업종명 기반 규칙(rules_default.json order 8)은 예시 키워드라 실데이터로 보정.
